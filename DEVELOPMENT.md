# 开发规范

## 编码与注释

- 所有源码、配置和文档统一使用 UTF-8 编码进行读取和写入。
- 新增或修改代码时，优先保持原文件编码风格；如无特殊情况，统一使用 UTF-8。
- 注释使用中文，内容简洁明确，只解释必要的设计意图和关键行为。

## 代码风格

- 优先遵循项目现有框架风格、命名习惯和目录结构。
- 实现应直接、简洁、高效，避免无意义的结构转换、重复封装和多层包装。
- 非必要不引入新的抽象层；能在当前层次清晰解决的问题，不额外拆分。
- 修改时以最小必要改动为原则，优先解决问题本身，不顺手扩大重构范围。
- 保持包内实现简洁，新增字段、函数和平台分支都必须有明确用途，避免给补丁遗留无用状态、重复实现和语义不清的代码。
- 基础包负责维护自身的运行时状态流转，业务层只做业务状态和协议状态映射，不把 API、数据库或页面语义反向耦合进基础包。
